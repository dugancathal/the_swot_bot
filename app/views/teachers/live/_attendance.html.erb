<%= form_tag submit_attendance_teachers_course_path(@course), remote: true do %>
  <%= render 'student_grid' %>
  <div class="attendance-button-container">
    <%= submit_tag 'Submit', class: 'btn btn-block btn-large' %>
    <%= link_to "Back", teachers_course_liveclass_path, :class => "btn btn-block btn-large" %>
  </div>
<% end %>
